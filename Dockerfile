FROM alpine:3.5

# dummy dockerfile which builds last on dockerhub,
# so that the root README.md is displayed as 
# repo info on:
# https://hub.docker.com/r/codeclou/docker-atlassian-confluence-data-center/

